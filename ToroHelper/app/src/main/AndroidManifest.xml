<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.toro.helper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WelcomActivity" android:theme="@style/NoTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MainActivity" android:theme="@style/NoTitle"/>
        <activity android:name=".activity.LoginActivity" android:theme="@style/NoTitle"/>
        <activity android:name=".activity.SmsCodeActivity" android:theme="@style/NoTitle"/>
        <activity android:name=".activity.ResetPassword" android:theme="@style/NoTitle"/>
        <activity android:name=".activity.RegisterActivity" android:theme="@style/NoTitle"/>
        <activity android:name=".activity.UploadPhotoActivity" android:theme="@style/NoTitle"/>
        <activity android:name=".activity.PhotoPreviewActivity" android:theme="@style/NoTitle"/>

        <!--<meta-data-->
            <!--android:name="com.toro.helper.utils.OkHttpGlideModule"-->
            <!--android:value="GlideModule" />-->

    </application>

</manifest>